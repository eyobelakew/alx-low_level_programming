#include "main.h"
#include <stdio.h>
/**
* _isdigit - checks for digit (0 through 9)
* @c: int to be checked
* Return: 1 if c is a digit, or 0
*/
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
